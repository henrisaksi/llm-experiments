[project]
name = "llm-experiments"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Henri Saksi", email = "henri.saksi@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "ipywidgets>=8.1.9",
    "jupyterlab>=4.6.3",
    "pillow>=12.3.0",
    "python-dotenv>=1.2.3",
    "torch>=2.14.0",
    "torchvision>=0.29.0",
    "transformers>=5.16.1",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
